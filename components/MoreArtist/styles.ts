import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    width: 140,
    margin: 10,
  },
  image: {
    width: '100%',
    height: 140,
    borderColor: '#FFFFFF',
    borderRadius: 150,
  },
  text: {
    color: 'white',
    marginTop: 10,
    fontSize: 14,
    textAlign: 'center',
  }
})

export default styles